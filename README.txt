run the server first:
      
      /vagrant/snek-multiplayer/ 
            npm run play

use another Terminal
run the player:
      
      /vagrant/snake-client/
            node play.js

    commands inside the game:
    w = Move: up
    a = Move: left
    s = Move: down
    d = Move: right

    send mensager to others player
    r = let's goooo
    t = it's growing up
    y = almost there
    e = It's Ed here
